#include <iostream>
#include <algorithm>
#include <stack>
using namespace std;

int main(void) {
    cin.tie(0); cout.tie(0); ios_base::sync_with_stdio(false);
    int N, L, min1, min2 = 1000000001;
    cin >> N;
    int arr[N];
    for (int i = 0; i < N; i++)
      cin >> arr[i];
    min1 = arr[0];
    for (int i = 0; i < N; i++) {
      
    }
}